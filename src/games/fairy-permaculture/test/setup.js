/**
 * Vitest setup for fairy-permaculture tests.
 *
 * Minimal JSDOM + Three.js stubs. Most game logic tests should not
 * need real WebGL; add stubs here as needed rather than pulling
 * in a real Three.js renderer.
 */

// Placeholder — extend as chunks need shared test fixtures.
